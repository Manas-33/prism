[project]
name = "prism"
version = "0.1.0"
description = "Blast-radius PR review: catches what your change breaks downstream and enforces your team's engineering rules, not just the diff."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6.2",
    "fastapi>=0.128.0",
    "google-genai>=1.61.0",
    "pyjwt[crypto]>=2.10.1",
    "python-dotenv>=1.2.1",
    "qdrant-client>=1.19.0",
    "redis>=7.1.0",
    "requests>=2.32.5",
    "slowapi>=0.1.9",
    "tree-sitter==0.20.4",
    "tree-sitter-languages>=1.10.2",
    "uvicorn>=0.40.0",
]

[tool.uv.workspace]
members = [
    "trial",
]
